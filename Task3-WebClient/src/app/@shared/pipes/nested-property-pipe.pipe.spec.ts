import { NestedPropertyPipePipe } from './nested-property-pipe.pipe';

describe('NestedPropertyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NestedPropertyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
